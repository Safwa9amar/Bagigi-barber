// Prisma schema for authentication system

generator client {
    provider = "prisma-client-js"
}

datasource db {
    provider = "mysql"
    url      = env("DATABASE_URL")
}


//////////////////////
// ENUMS
//////////////////////

enum BookingStatus {
    PENDING // في الطابور
    IN_PROGRESS // جاري
    DONE // انتهى
    CANCELLED
}
