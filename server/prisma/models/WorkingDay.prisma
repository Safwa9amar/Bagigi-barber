model WorkingDay {
  id        String  @id @default(uuid())
  userId    String
  day       Int
  startTime String
  endTime   String
  isOpen    Boolean @default(true)
  user      User    @relation(fields: [userId], references: [id])

  @@index([userId], map: "WorkingDay_userId_fkey")
}
